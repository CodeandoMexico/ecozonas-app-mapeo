class TaskModel {
  TaskModel({
    required this.title,
    required this.detail,
    required this.percentage,
    required this.type,
  });

  String title;
  String detail;
  double percentage;
  String type;
}